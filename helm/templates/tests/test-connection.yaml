apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "library-management-system.fullname" . }}-test-connection"
  labels:
    {{- include "library-management-system.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "library-management-system.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
